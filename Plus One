class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        x=0
        i=0
        l=len(digits)-1
        while i<len(digits):
            x+=digits[i]*pow(10,l)
            l=l-1
            i+=1
        y=(x+1)
        z=[d for d in str(y)]
        return (z)

        
